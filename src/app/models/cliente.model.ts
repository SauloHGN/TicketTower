export class Cliente {
  id: number;
  usuarioID: number;
  IDEmpresa: number;

  constructor(id: number, usuarioID: number, IDEmpresa: number) {
    this.id = id;
    this.usuarioID = usuarioID;
    this.IDEmpresa = IDEmpresa;
  }
}
